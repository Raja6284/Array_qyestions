//Leaders in an arry

//An element is a leader if it is greater than all the elements to its right side and the right most element is always a leader

#include <iostream>
#include<vector>
using namespace std;

vector<int> leadersInArray(vector<int> arr, int n){

    
}
int main(){
    vector<int> arr;

    arr = {16,17,4,3,5,2};

    leadersInArray(arr, arr.size());

    for(int i = 0; i<arr.size();i++){
          cout<<arr[i]<<" ";
    }
  
    return 0;
}